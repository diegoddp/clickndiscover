name: Deploy Static Site via SSH

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare deploy package
        run: |
          mkdir -p deploy/brainwave
          mkdir -p deploy/keyslim
          mkdir -p deploy/prostadine
          cp "index.html" deploy/
          cp "robots.txt" deploy/
          cp "sitemap.xml" deploy/
          cp "brainwave/index.html" deploy/brainwave/
          cp "brainwave/billionaire-brain-wave-desktop.png" deploy/brainwave/
          cp "brainwave/billionaire-brain-wave-mobile.png" deploy/brainwave/
          if [ -f "brainwave/terms.html" ]; then cp "brainwave/terms.html" deploy/brainwave/; fi
          if [ -f "brainwave/privacy.html" ]; then cp "brainwave/privacy.html" deploy/brainwave/; fi
          cp "keyslim/index.html" deploy/keyslim/
          cp "keyslim/keyslim-desktop.png" deploy/keyslim/
          cp "keyslim/keyslim-mobile.png" deploy/keyslim/
          cp "keyslim/favicon.svg" deploy/keyslim/
          cp "keyslim/terms.html" deploy/keyslim/
          cp "keyslim/privacy.html" deploy/keyslim/
          cp "prostadine/index.html" deploy/prostadine/
          cp "prostadine/prostadine-desktop.png" deploy/prostadine/
          cp "prostadine/prostadine-mobile.png" deploy/prostadine/
          cp "prostadine/favicon.svg" deploy/prostadine/
          cp "prostadine/terms.html" deploy/prostadine/
          cp "prostadine/privacy.html" deploy/prostadine/
          mkdir -p deploy/avatar-ia-lucrativo/depoimentos
          cp "avatar-ia-lucrativo/index.html" deploy/avatar-ia-lucrativo/
          cp "avatar-ia-lucrativo/favicon.svg" deploy/avatar-ia-lucrativo/
          cp "avatar-ia-lucrativo/privacy.html" deploy/avatar-ia-lucrativo/
          cp "avatar-ia-lucrativo/terms.html" deploy/avatar-ia-lucrativo/
          cp "avatar-ia-lucrativo/foto-autor.jpg" deploy/avatar-ia-lucrativo/
          cp "avatar-ia-lucrativo/hero-mockup-combo.png" deploy/avatar-ia-lucrativo/
          cp "avatar-ia-lucrativo/Selo_de_Garantia_de_7_Dias_PNG_Transparente_Sem_Fundo.png" deploy/avatar-ia-lucrativo/
          cp avatar-ia-lucrativo/depoimentos/*.png deploy/avatar-ia-lucrativo/depoimentos/

      - name: Clean target folder on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            mkdir -p "${{ secrets.SSH_TARGET_PATH }}"
            rm -rf "${{ secrets.SSH_TARGET_PATH }}"/*

      - name: Deploy site to server via SCP
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          source: "deploy/"
          target: ${{ secrets.SSH_TARGET_PATH }}
          strip_components: 1

